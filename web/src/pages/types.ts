/**
 * Re-export van `shared/`, zodat de pagina's hun bestaande imports houden en
 * er maar één definitie van deze vormen bestaat.
 */
export type {
  CallClassification,
  CallPerson,
  RunCall,
  RunStatus,
  Worker,
} from '@shared'
